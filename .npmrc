# @taoist-labs/components 的 peer 仍为 @seedao/sns-js；项目使用 @seedao2.0/sns-js（见 package.json overrides）
# npm 7+：peer 冲突时放宽解析
legacy-peer-deps=true
# @seedao2.0/* 在部分国内镜像上元数据/包不完整，npm 可能误报 workspace:* 或 404；该 scope 固定走官方源
@seedao2.0:registry=https://registry.npmjs.org/
# pnpm 专用项已挪到 package.json 的 "pnpm" 字段，避免 npm 报 Unknown project config
