# @taoist-labs/components 依赖 @seedao/sns-js；package.json 用 npm 别名将其指向 @seedao2.0/sns-js
# npm 7+：peer 冲突时放宽解析
legacy-peer-deps=true
# @seedao2.0/* 在部分国内镜像上元数据/包不完整，npm 可能误报 workspace:* 或 404；该 scope 固定走官方源
@seedao2.0:registry=https://registry.npmjs.org/
# Netlify 构建见 netlify.toml 的 NPM_CONFIG_REGISTRY（全量走官方源，避免镜像缺包）
